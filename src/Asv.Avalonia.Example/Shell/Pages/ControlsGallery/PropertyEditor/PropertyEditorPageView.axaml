<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:example="clr-namespace:Asv.Avalonia.Example"
    mc:Ignorable="d"
    d:DesignWidth="800"
    d:DesignHeight="450"
    x:Class="Asv.Avalonia.Example.PropertyEditorPageView"
    x:DataType="example:PropertyEditorPageViewModel"
>
    <Design.DataContext>
        <example:PropertyEditorPageViewModel />
    </Design.DataContext>
    <Grid ColumnDefinitions="*,*">
        <DockPanel Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Text="PropertyEditor" />
            <ContentControl MinWidth="250" Content="{Binding PropertyEditor}" />
        </DockPanel>

        <DockPanel LastChildFill="False" Grid.Column="1">
            <TextBlock DockPanel.Dock="Top" Text="Display name" />
            <TextBox DockPanel.Dock="Top" Text="{Binding DisplayNameProperty.Text.Value}" />
            <TextBlock DockPanel.Dock="Top" Text="Operation profile" />
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding OperationProfileProperty.SelectedItem.Value.Header}"
            />
            <TextBlock DockPanel.Dock="Top" Text="Run check count" />
            <TextBlock DockPanel.Dock="Top" Text="{Binding ActionButtonClickCount}" />
            <TextBlock DockPanel.Dock="Top" Text="Altitude V2" />
            <TextBlock DockPanel.Dock="Top" Text="{Binding AltitudeUnitProperty.Text.Value}" />
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding AltitudeUnitProperty.Unit.CurrentUnitItem.Value.Name}"
            />
            <TextBlock DockPanel.Dock="Top" Text="Throttle V2" />
            <TextBlock DockPanel.Dock="Top" Text="{Binding ThrottleUnitProperty.Text.Value}" />
            <TextBlock
                DockPanel.Dock="Top"
                Text="{Binding ThrottleUnitProperty.Unit.CurrentUnitItem.Value.Name}"
            />
            <TextBlock DockPanel.Dock="Top" Text="Latitude" />
            <TextBox DockPanel.Dock="Top" Text="{Binding Latitude.Value}" />
            <TextBlock DockPanel.Dock="Top" Text="Longitude" />
            <TextBox DockPanel.Dock="Top" Text="{Binding Longitude.Value}" />
            <TextBlock DockPanel.Dock="Top" Text="Altitude" />
            <TextBox DockPanel.Dock="Top" Text="{Binding Altitude.Value}" />
            <TextBlock DockPanel.Dock="Top" Text="GeoPoint" />
            <TextBlock DockPanel.Dock="Top" Text="{Binding GeoPoint.Value}" />
        </DockPanel>
    </Grid>
</UserControl>
