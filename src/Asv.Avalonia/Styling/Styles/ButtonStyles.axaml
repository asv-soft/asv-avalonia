<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style Selector="Button">
       
        <!-- Template target (ContentPresenter внутри кнопки) -->
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
            <!-- Базовые стили кнопки (по умолчанию) остаются от темы -->
        </Style>

        <!-- ========== STATUS BUTTONS ========== -->
        <Style Selector="^.error /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundErrorBrush}"/>
            <Setter Property="BorderBrush"   Value="{DynamicResource AsvForegroundErrorBrush}"/>
            <Setter Property="Foreground"   Value="{DynamicResource AsvForegroundErrorBrush}"/>
        </Style>

        <Style Selector="^.warning /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundWarningBrush}"/>
            <Setter Property="BorderBrush"   Value="{DynamicResource AsvForegroundWarningBrush}"/>
            <Setter Property="Foreground"   Value="{DynamicResource AsvForegroundWarningBrush}"/>
        </Style>

        <Style Selector="^.success /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundSuccessBrush}"/>
            <Setter Property="BorderBrush"   Value="{DynamicResource AsvForegroundSuccessBrush}"/>
            <Setter Property="Foreground"   Value="{DynamicResource AsvForegroundSuccessBrush}"/>
        </Style>

        <Style Selector="^.unknown /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundUnknownBrush}"/>
            <Setter Property="BorderBrush"   Value="{DynamicResource AsvForegroundUnknownBrush}"/>
            <Setter Property="Foreground"   Value="{DynamicResource AsvForegroundUnknownBrush}"/>
        </Style>

        <!-- ========== INFO PALETTE BUTTONS (1–20) ========== -->
        <Style Selector="^.info1  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo1Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo1Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo1Brush}"/>
        </Style>
        <Style Selector="^.info2  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo2Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo2Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo2Brush}"/>
        </Style>
        <Style Selector="^.info3  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo3Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo3Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo3Brush}"/>
        </Style>
        <Style Selector="^.info4  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo4Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo4Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo4Brush}"/>
        </Style>

        <Style Selector="^.info5  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo5Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo5Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo5Brush}"/>
        </Style>
        <Style Selector="^.info6  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo6Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo6Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo6Brush}"/>
        </Style>
        <Style Selector="^.info7  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo7Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo7Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo7Brush}"/>
        </Style>
        <Style Selector="^.info8  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo8Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo8Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo8Brush}"/>
        </Style>
        <Style Selector="^.info9  /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo9Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo9Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo9Brush}"/>
        </Style>
        <Style Selector="^.info10 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo10Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo10Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo10Brush}"/>
        </Style>

        <Style Selector="^.info11 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo11Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo11Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo11Brush}"/>
        </Style>
        <Style Selector="^.info12 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo12Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo12Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo12Brush}"/>
        </Style>
        <Style Selector="^.info13 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo13Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo13Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo13Brush}"/>
        </Style>
        <Style Selector="^.info14 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo14Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo14Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo14Brush}"/>
        </Style>
        <Style Selector="^.info15 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo15Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo15Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo15Brush}"/>
        </Style>
        <Style Selector="^.info16 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo16Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo16Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo16Brush}"/>
        </Style>
        <Style Selector="^.info17 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo17Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo17Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo17Brush}"/>
        </Style>
        <Style Selector="^.info18 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo18Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo18Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo18Brush}"/>
        </Style>
        <Style Selector="^.info19 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo19Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo19Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo19Brush}"/>
        </Style>
        <Style Selector="^.info20 /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundInfo20Brush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AsvForegroundInfo20Brush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundInfo20Brush}"/>
        </Style>
    </Style>
    
    <Style Selector="Button.danger">
        <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource AsvBackgroundWarningBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource AsvForegroundWarningBrush}" />
        </Style>
    </Style>
</Styles>